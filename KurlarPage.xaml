<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="odev3gorsel.KurlarPage"
             Title="Döviz Kurları"
             BackgroundColor="{AppThemeBinding Light=#E0F2F1, Dark=#1a1a1a}">

    <Grid RowDefinitions="Auto, Auto, *">
        
        <!-- Başlık -->
        <Frame Grid.Row="0" BackgroundColor="{AppThemeBinding Light=#00897B, Dark=#004D40}" CornerRadius="0" Padding="15" HasShadow="True">
            <VerticalStackLayout>
                <Label Text="💱 Güncel Döviz Kurları" 
                       TextColor="White" 
                       FontSize="20"
                       FontAttributes="Bold" 
                       HorizontalOptions="Center"/>
                <Label Text="Canlı piyasa verileri" 
                       TextColor="{AppThemeBinding Light=#B2DFDB, Dark=#80CBC4}" 
                       FontSize="12"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Frame>

        <!-- Yükleme Göstergesi -->
        <ActivityIndicator x:Name="LoadingIndicator" 
                           Grid.Row="1" 
                           IsRunning="True" 
                           IsVisible="False" 
                           VerticalOptions="Center" 
                           HorizontalOptions="Center" 
                           Color="{AppThemeBinding Light=#00897B, Dark=#4DB6AC}"/>

        <!-- Tablo Başlığı -->
        <Frame Grid.Row="1" BackgroundColor="{AppThemeBinding Light=#B2DFDB, Dark=#004D40}" CornerRadius="0" Padding="12" HasShadow="False" Margin="10,10,10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="70" />
                </Grid.ColumnDefinitions>
                <Label Text="💰 Tür" FontAttributes="Bold" Grid.Column="0" TextColor="{AppThemeBinding Light=#00695C, Dark=#B2DFDB}"/>
                <Label Text="📈 Alış" FontAttributes="Bold" Grid.Column="1" TextColor="{AppThemeBinding Light=#00695C, Dark=#B2DFDB}"/>
                <Label Text="📉 Satış" FontAttributes="Bold" Grid.Column="2" TextColor="{AppThemeBinding Light=#00695C, Dark=#B2DFDB}"/>
                <Label Text="📊 Fark" FontAttributes="Bold" Grid.Column="3" TextColor="{AppThemeBinding Light=#00695C, Dark=#B2DFDB}"/>
            </Grid>
        </Frame>

        <!-- Kur Listesi -->
        <CollectionView x:Name="KurlarList" Grid.Row="2" Margin="10,5,10,10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}" CornerRadius="10" Padding="12" Margin="0,5" HasShadow="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding Isim}" VerticalOptions="Center" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#00695C, Dark=#4DB6AC}"/>
                            <Label Text="{Binding Alis}" Grid.Column="1" VerticalOptions="Center" TextColor="{AppThemeBinding Light=#424242, Dark=#cccccc}"/>
                            <Label Text="{Binding Satis}" Grid.Column="2" VerticalOptions="Center" TextColor="{AppThemeBinding Light=#424242, Dark=#cccccc}"/>
                            <Frame Grid.Column="3" BackgroundColor="{AppThemeBinding Light=#E8F5E9, Dark=#1B5E20}" CornerRadius="8" Padding="5,2" HasShadow="False">
                                <Label Text="{Binding Fark}" VerticalOptions="Center" TextColor="{AppThemeBinding Light=#2E7D32, Dark=#A5D6A7}" HorizontalOptions="Center" FontSize="12"/>
                            </Frame>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>