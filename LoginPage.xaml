<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="odev3gorsel.LoginPage"
             Title="Giriş Yap"
             BackgroundColor="{AppThemeBinding Light=#E0F2F1, Dark=#1a1a1a}">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">

            <!-- Logo/İkon Alanı -->
            <Frame BackgroundColor="{AppThemeBinding Light=#00897B, Dark=#004D40}" 
                   CornerRadius="50" 
                   HeightRequest="100" 
                   WidthRequest="100" 
                   HorizontalOptions="Center"
                   HasShadow="True"
                   Padding="0">
                <Label Text="🔐" 
                       FontSize="48" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center"/>
            </Frame>

            <!-- Giriş Formu -->
            <Frame BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}" CornerRadius="20" Padding="25" HasShadow="True">
                <VerticalStackLayout Spacing="18">
                    
                    <Label Text="Hoş Geldiniz" 
                           FontSize="28" 
                           HorizontalOptions="Center" 
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light=#00695C, Dark=#4DB6AC}"/>

                    <Label Text="Hesabınıza giriş yapın" 
                           FontSize="14" 
                           HorizontalOptions="Center" 
                           TextColor="{AppThemeBinding Light=#757575, Dark=#aaaaaa}"/>

                    <!-- E-posta Girişi -->
                    <Frame BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#3d3d3d}" CornerRadius="12" Padding="5" HasShadow="False">
                        <Entry x:Name="LoginEmail" 
                               Placeholder="📧  E-posta adresiniz" 
                               Keyboard="Email"
                               FontSize="16"
                               TextColor="{AppThemeBinding Light=#333333, Dark=White}"
                               PlaceholderColor="{AppThemeBinding Light=#999999, Dark=#888888}"/>
                    </Frame>

                    <!-- Şifre Girişi -->
                    <Frame BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#3d3d3d}" CornerRadius="12" Padding="5" HasShadow="False">
                        <Entry x:Name="LoginPassword" 
                               Placeholder="🔒  Şifreniz" 
                               IsPassword="True"
                               FontSize="16"
                               TextColor="{AppThemeBinding Light=#333333, Dark=White}"
                               PlaceholderColor="{AppThemeBinding Light=#999999, Dark=#888888}"/>
                    </Frame>

                    <!-- Giriş Butonu -->
                    <Button Text="Giriş Yap" 
                            Clicked="BtnLogin_Clicked" 
                            BackgroundColor="{AppThemeBinding Light=#00897B, Dark=#26A69A}" 
                            TextColor="White"
                            FontAttributes="Bold"
                            FontSize="18"
                            CornerRadius="12"
                            HeightRequest="50"/>

                    <!-- Ayraç -->
                    <BoxView HeightRequest="1" BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#444444}" Margin="20,5"/>

                    <!-- Kayıt Ol Butonu -->
                    <Button Text="Hesabınız yok mu? Kayıt Olun" 
                            Clicked="BtnGoToRegister_Clicked" 
                            BackgroundColor="Transparent" 
                            TextColor="{AppThemeBinding Light=#00897B, Dark=#4DB6AC}"
                            FontSize="14"/>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>